<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug-only override: allow emulator host for local backend via http://10.0.2.2 -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>

    <!-- Keep the production domain locked down as in main -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="false">vlsi-booking-app.onrender.com</domain>
    </domain-config>
</network-security-config>
